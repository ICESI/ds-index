docker run --rm -it -v $(pwd):/temporal --name ubuntu_apache2 ubuntu /bin/bash
cp /etc/apt/sources.list /temporal
docker build -t ubuntu_apache2 .
docker run -d -v $(pwd)/src:/var/www/html/ -p 8080:80 --name apache2_container ubuntu_apache2
